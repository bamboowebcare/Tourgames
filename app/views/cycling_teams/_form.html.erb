<%= form_for(@cycling_team) do |f| %>
  <% if @cycling_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cycling_team.errors.count, "error") %> prohibited this cycling_team from being saved:</h2>

      <ul>
      <% @cycling_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :logo %><br>
    <%= f.text_area :logo %>
  </div>
  <div class="field">
    <%= f.label :tdf %><br>
    <%= f.check_box :tdf %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
